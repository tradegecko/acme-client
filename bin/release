#!/usr/bin/env ruby

require 'bundler/setup'
require 'acmev1-client'

version = AcmeV1::Client::VERSION

def `(command)
  puts super(command)

  if $?.exitstatus > 0
    fail("failed at: #{command}")
  end
end

`git add lib/acmev1/client/version.rb`
`git commit -m "bump to #{version}"`
`git pull --rebase origin master`
`git tag 'v#{version}'`
`git push --tags origin master`
`gem build acmev1-client.gemspec`
`gem push acmev1-client-#{version}.gem`
`rm acmev1-client-#{version}.gem`
